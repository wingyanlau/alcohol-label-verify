name: Deploy Staging

# main is the staging line: every merge lands on
# https://alcohol-label-verify-staging.wing-lawrence.workers.dev behind the same
# quality gate a pull request faces. Production is a separate branch (see
# deploy-production.yml), so a merge to main is never a production event.
on:
  push:
    branches: [main]
  workflow_dispatch:

# One deploy at a time, and a superseded one is cancelled rather than queued:
# two concurrent runs would race on the D1 migration and on the worker upload.
concurrency:
  group: deploy-staging
  cancel-in-progress: true

jobs:
  quality:
    uses: ./.github/workflows/quality.yml

  deploy:
    name: Deploy Staging
    needs: quality
    runs-on: ubuntu-latest
    environment:
      name: staging
      url: https://alcohol-label-verify-staging.wing-lawrence.workers.dev
    permissions:
      contents: read
      deployments: write
    # Credentials are held by the `staging` GitHub environment rather than at
    # repository scope, so a job that does not declare an environment cannot
    # resolve them at all. Declared once here instead of per step: there is then
    # a single place where an environment's credentials enter the job, and no
    # way for a later step to quietly reach for a different set.
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      # The door on the deployment (D49). Every probe below goes through it,
      # including the cheap ones — a health check that calls a model is the
      # cheapest way to spend the quota the gate exists to protect.
      #
      # Two pairs, so two people can evaluate at once and either can be revoked
      # without locking out the other. CI uses the first.
      POC_USER_ONE: ${{ secrets.POC_USER_ONE }}
      POC_USER_ONE_PASSWORD: ${{ secrets.POC_USER_ONE_PASSWORD }}
      POC_USER_TWO: ${{ secrets.POC_USER_TWO }}
      POC_USER_TWO_PASSWORD: ${{ secrets.POC_USER_TWO_PASSWORD }}
    steps:
      # First, and before the checkout: a missing environment secret otherwise
      # surfaces as an opaque wrangler auth error several minutes in. Now that
      # each environment carries its own set, an environment added later with
      # one key forgotten is the plausible failure.
      - name: Credentials resolved
        run: |
          set -euo pipefail
          : "${CLOUDFLARE_API_TOKEN:?missing — add it under Settings → Environments → staging}"
          : "${CLOUDFLARE_ACCOUNT_ID:?missing — add it under Settings → Environments → staging}"

      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # Migrations run before the upload, so the new code never meets the old
      # schema. The audit_event table is append-only (D32), which is what makes
      # this order safe: a migration adds, it does not rewrite history.
      - name: Apply D1 migrations
        run: npm run migrate:staging

      # No `wrangler secret bulk` step, unlike sibling projects: Workers AI
      # authenticates through its binding, so this deployment holds no secrets
      # at all. /health knows that and does not ask for MODEL_API_KEY.
      #
      # Retried once. The Cloudflare API can return a 521 with an HTML body on
      # a trailing call — observed on run 30780074509, where the script had
      # already uploaded and the deployment reached 100%, and only the
      # subdomain registration failed. `wrangler deploy` is idempotent, so a
      # second attempt costs an upload and settles whether the failure was the
      # revision or the API. The verification below is the real arbiter either
      # way: if the deploy did land, it passes; if it did not, it fails.
      # Push the provider credential BEFORE deploying.
      #
      # Order matters more than it looks: `wrangler secret put` publishes a new
      # version of the worker. Setting it after the deploy would make the
      # version id captured below stale, and the verification would wait for a
      # version that is no longer the live one.
      #
      # The value travels through the environment rather than being
      # interpolated into the command line: a key containing a quote or a
      # backslash would break an inline JSON payload, and a command line is a
      # worse place for a credential than a variable.
      #
      # GitHub names the secret after the vendor; the worker names it after the
      # role it plays, because which vendor fills it is configuration
      # (MODEL_PROVIDER) rather than a fact about the code.
      - name: Push the provider credential
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          set -euo pipefail
          if [ -z "${GEMINI_API_KEY:-}" ]; then
            echo "::notice::no GEMINI_API_KEY for this environment — worker secrets left untouched"
            exit 0
          fi
          printf '%s' "$GEMINI_API_KEY" | npx wrangler secret put MODEL_API_KEY --env staging

      # The gateway's address is half configuration and half account
      # identifier. The name is committed; the account is not, because this
      # repository is meant to be readable and a name on its own addresses
      # nothing. Reuses the account id CI already holds — no new secret.
      - name: Push the gateway account
        run: |
          set -euo pipefail
          if printf '%s' "$CLOUDFLARE_ACCOUNT_ID" \
               | npx wrangler secret put AI_GATEWAY_ACCOUNT --env staging 2>&1 | tee /tmp/secret.log
          then
            exit 0
          fi

          # A name cannot be a var and a secret at once, so the first run of a
          # var-to-secret migration collides with the value still deployed
          # (code 10053). The deploy below removes the var, and the next run
          # installs the secret — the gateway is simply unrouted in between,
          # which /health reports rather than hiding.
          if grep -q '10053' /tmp/secret.log; then
            echo "::warning::AI_GATEWAY_ACCOUNT is still a deployed var; the secret lands on the next run"
            exit 0
          fi
          echo "::error::could not set AI_GATEWAY_ACCOUNT"
          exit 1

      # Only needed when the gateway has authentication enabled. Absent means
      # the gateway is open and nothing is written, rather than an empty token
      # being installed over a working one.
      - name: Push the gateway token
        env:
          AI_GATEWAY_TOKEN: ${{ secrets.AI_GATEWAY_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${AI_GATEWAY_TOKEN:-}" ]; then
            echo "::notice::no AI_GATEWAY_TOKEN for this environment — assuming an open gateway"
            exit 0
          fi
          printf '%s' "$AI_GATEWAY_TOKEN" | npx wrangler secret put AI_GATEWAY_TOKEN --env staging

      # The reviewer credentials, installed as worker secrets.
      #
      # None configured means the deployment stays open, which is the honest
      # behaviour for a repository somebody has just forked: a gate that closed
      # without a credential would leave them with a 401 and no way in. Which of
      # the two states this deploy left is reported, not assumed.
      #
      # Each pair is installed only if BOTH halves are present. Half a pair
      # would otherwise install a username whose password is whatever was left
      # deployed from a previous run.
      - name: Push the reviewer credentials
        run: |
          set -euo pipefail
          installed=0
          put() {
            name="$1"; user="$2"; password="$3"
            if [ -z "$user" ] || [ -z "$password" ]; then
              echo "::notice::$name incomplete or unset — not installed"
              return 0
            fi
            printf '%s' "$user" | npx wrangler secret put "${name}" --env staging
            printf '%s' "$password" | npx wrangler secret put "${name}_PASSWORD" --env staging
            installed=$((installed + 1))
          }
          put POC_USER_ONE "${POC_USER_ONE:-}" "${POC_USER_ONE_PASSWORD:-}"
          put POC_USER_TWO "${POC_USER_TWO:-}" "${POC_USER_TWO_PASSWORD:-}"
          if [ "$installed" -eq 0 ]; then
            echo "::warning::no reviewer credential configured — this deployment is OPEN to anyone with the URL"
          else
            echo "::notice::$installed reviewer credential(s) installed; the deployment is gated"
          fi

      # How every probe below authenticates.
      #
      # A netrc file rather than -u: the credential then never appears in a
      # process argument list, and a password containing a space survives the
      # unquoted expansion these steps use. Empty when nothing is configured,
      # which is exactly the open-deployment case.
      - name: Prepare the probe credential
        run: |
          set -euo pipefail
          # Whichever pair is complete, preferring the first. Taking only pair
          # one would fail every probe on a deployment gated by pair two alone
          # — a real configuration, and one where the deploy would look broken
          # rather than gated.
          user=""; password=""
          if [ -n "${POC_USER_ONE:-}" ] && [ -n "${POC_USER_ONE_PASSWORD:-}" ]; then
            user="$POC_USER_ONE"; password="$POC_USER_ONE_PASSWORD"
          elif [ -n "${POC_USER_TWO:-}" ] && [ -n "${POC_USER_TWO_PASSWORD:-}" ]; then
            user="$POC_USER_TWO"; password="$POC_USER_TWO_PASSWORD"
          fi
          if [ -z "$user" ]; then
            echo "GATE_AUTH=" >> "$GITHUB_ENV"
            exit 0
          fi
          netrc="$RUNNER_TEMP/gate.netrc"
          umask 077
          printf 'machine %s login %s password %s\n' \
            "alcohol-label-verify-staging.wing-lawrence.workers.dev" \
            "$user" "$password" > "$netrc"
          echo "GATE_AUTH=--netrc-file $netrc" >> "$GITHUB_ENV"

      - name: Deploy
        id: deploy
        run: |
          set -euo pipefail
          deploy() { npm run deploy:staging 2>&1; }
          # Retried once: the Cloudflare API can return a 521 with an HTML body
          # on a trailing call, after the script has already uploaded. `wrangler
          # deploy` is idempotent, so a second attempt settles whether the
          # failure was the revision or the API.
          out=$(deploy) || {
            echo "::warning::first deploy attempt failed — retrying once"
            sleep 15
            out=$(deploy)
          }
          echo "$out"

          # The version this run published. Verification below waits for THIS
          # id to be the one answering, rather than accepting whichever version
          # happens to reply first.
          version=$(printf '%s\n' "$out" | grep -oE 'Current Version ID: [0-9a-fA-F-]{8,}' | tail -1 | sed 's/.*: //')
          [ -n "$version" ] || { echo "::error::could not read the deployed version id"; exit 1; }
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "deployed version $version"

      # A deploy can succeed with a binding silently missing, and /health is the
      # only thing that notices — it returns 503 with the offending setting
      # named. `environment` is asserted too, because a correct-looking deploy
      # of the wrong env block is the failure this catches that a 200 does not.
      - name: Verify deployment
        run: |
          set -euo pipefail
          url="https://alcohol-label-verify-staging.wing-lawrence.workers.dev"
          want="${{ steps.deploy.outputs.version }}"

          # Wait for the version just deployed to be the one answering.
          #
          # Cloudflare serves the previous worker for a few seconds after
          # upload. The old loop retried until it received a 200 and then
          # asserted — which meant a healthy PREVIOUS version could satisfy a
          # check about a broken new one. That is not a hypothetical: a
          # deployment misconfigured for its provider passed this gate.
          #
          # No -f, deliberately: a misconfigured deployment answers 503, and its
          # body is exactly what has to be read to know it arrived.
          body=''
          got=''
          for attempt in $(seq 1 15); do
            body=$(curl -sS $GATE_AUTH --max-time 20 "$url/health" || true)
            got=$(printf '%s' "$body" | jq -r '.version // ""' 2>/dev/null || echo '')
            [ "$got" = "$want" ] && break
            echo "attempt $attempt: serving version ${got:-<none>}, waiting for $want"
            sleep 4
          done

          [ "$got" = "$want" ] || {
            echo "::error::the deployed version never became live (still serving ${got:-<none>})"
            exit 1
          }

          echo "$body"
          echo "$body" | jq -e '.status == "ok"' > /dev/null \
            || { echo "::error::/health reported: $(echo "$body" | jq -c '.problems // .')"; exit 1; }
          echo "$body" | jq -e '.environment == "staging"' > /dev/null \
            || { echo "::error::deployed worker reports environment $(echo "$body" | jq -r '.environment')"; exit 1; }

          # modelApiKey is absent by design under a binding-authed provider —
          # asserting that every binding is true would fail such a deploy. These
          # five are the ones whose absence a successful upload would hide.
          missing=$(echo "$body" | jq -r '
            .bindings as $b
            | ["staging", "database", "inference", "workQueue", "jobCoordinator"]
            | map(select($b[.] != true)) | join(", ")')
          [ -z "$missing" ] || { echo "::error::missing bindings: $missing"; exit 1; }

          # Cloudflare propagation lags a few seconds; retry before diagnosing.
          for attempt in 1 2 3 4 5; do
            if body=$(curl -fsS $GATE_AUTH --max-time 20 "$url/health"); then break; fi
            echo "attempt $attempt: /health not healthy yet"
            body=$(curl -sS $GATE_AUTH --max-time 20 "$url/health" || true)
            sleep 5
          done

          echo "$body"
          echo "$body" | jq -e '.status == "ok"' > /dev/null \
            || { echo "::error::/health reported: $(echo "$body" | jq -c '.problems // .')"; exit 1; }
          echo "$body" | jq -e '.environment == "staging"' > /dev/null \
            || { echo "::error::deployed worker reports environment $(echo "$body" | jq -r '.environment')"; exit 1; }

          # modelApiKey is absent by design and reports false — asserting that
          # every binding is true would fail every deploy. These five are the
          # ones whose absence a successful upload would otherwise hide.
          missing=$(echo "$body" | jq -r '
            .bindings as $b
            | ["staging", "database", "inference", "workQueue", "jobCoordinator"]
            | map(select($b[.] != true)) | join(", ")')
          [ -z "$missing" ] || { echo "::error::missing bindings: $missing"; exit 1; }

      # The inference binding is exercised separately because it is the one
      # dependency a config check cannot prove: the binding can be present and
      # the model still unreachable. Whether the model can *read* a 4.5pt
      # warning is a corpus measurement (B-Q4), not this.
      # The policy archive is brought current in the same place, and for the
      # same reason, as the schema: both have to be right before the revision
      # serves a request. Reconciliation is idempotent, so a deploy that
      # changed no rule reports "already agrees" and writes nothing.
      # The policy archive is brought current in the same place, and for the
      # same reason, as the schema: both have to be right before the revision
      # serves a request. Reconciliation is idempotent, so a deploy that
      # changed no rule reports "already agrees" and writes nothing.
      #
      # Retried, because a route that is new in this revision is not reachable
      # the instant the version check passes. Cloudflare serves the previous
      # worker for a few seconds afterwards, and that worker answers an unknown
      # path with a perfectly valid `{"error":"not_found"}` — which the first
      # version of this step read as "no actions" and failed on. The body was
      # right there in the log saying not_found, and the assertion was written
      # to look past it.
      - name: Reconcile the policy archive
        run: |
          set -euo pipefail
          url="https://alcohol-label-verify-staging.wing-lawrence.workers.dev"

          # The digest of the set THIS commit ships. A reconciliation reporting
          # any other digest was answered by a worker still serving the previous
          # version — which happens during propagation, answers 200, and says
          # truthfully that the archive already agrees, because against its own
          # bundle it does. Retrying on the status code alone cannot see that.
          want=$(jq -r '.contentDigest' config/policy-set.json)
          echo "expecting policy set digest $want"

          for attempt in $(seq 1 10); do
            code=$(curl -sS $GATE_AUTH --max-time 60 -X POST "$url/policy/reconcile" \
                     -o /tmp/reconcile.json -w '%{http_code}') || true
            got=$(jq -r '.contentDigest // empty' /tmp/reconcile.json 2>/dev/null || true)
            if [ "$code" = "200" ] && [ "$got" = "$want" ]; then
              jq '{policySetVersion, contentDigest, actions, summary, events}' /tmp/reconcile.json
              exit 0
            fi
            echo "attempt $attempt: http $code, digest '${got:-none}' — waiting for $want"
            sleep 6
          done

          # Named, not inferred. A reconciliation that could not run leaves the
          # deployment enforcing rules nobody reviewed, and the next step
          # refuses that — but the cause belongs here.
          echo "::error::no reconciliation from the version just deployed after 10 attempts"
          cat /tmp/reconcile.json
          exit 1

      # Drift between the reviewed file and the rows is a deployment problem,
      # not a request failure — the rules being enforced would not be the ones
      # anybody approved. /health/policy answers 503 when they disagree.
      - name: Verify the archive matches what was reviewed
        run: |
          set -euo pipefail
          url="https://alcohol-label-verify-staging.wing-lawrence.workers.dev"
          body=$(curl -sS $GATE_AUTH --max-time 60 "$url/health/policy") || true
          echo "$body" | jq '{rows, pending, inSync, activeInFile, draftsInFile}' || echo "$body"
          echo "$body" | jq -e '.inSync == true' >/dev/null \
            || { echo "::error::policy archive does not match the reviewed file"; exit 1; }

      - name: Verify inference reachable
        run: |
          set -euo pipefail
          url="https://alcohol-label-verify-staging.wing-lawrence.workers.dev"
          body=$(curl -sS $GATE_AUTH --max-time 60 "$url/health/inference" || true)
          echo "$body"

          if echo "$body" | jq -e '.status == "ok"' > /dev/null 2>&1; then
            exit 0
          fi

          # The provider classifies its own failure and reports it; this reads
          # that verdict rather than matching error text.
          #
          # The previous version grepped for "4006", "daily free allocation" and
          # "neurons" — Cloudflare's vocabulary, in a workflow that deploys
          # whichever provider is configured. Pointed at Gemini it failed a
          # sound revision for an exhausted account quota, which is exactly the
          # leak the provider abstraction was built to close and which survived
          # in CI after being fixed in the code.
          fault=$(echo "$body" | jq -r '.fault // "unknown"' 2>/dev/null || echo unknown)
          reason=$(echo "$body" | jq -r '.error // .reason // "unknown"' 2>/dev/null || echo unknown)

          case "$fault" in
            quota-exhausted|rate-limited)
              echo "::warning::inference unverified ($fault): $reason"
              echo "::warning::this is an account condition, not a problem with this revision"
              ;;
            *)
              echo "::error::/health/inference reported [$fault]: $reason"
              exit 1
              ;;
          esac

      # The other dependency no configuration check can prove. Rasterisation is
      # the batch path's entry point (D33) — the model is never shown a PDF text
      # layer, so a browser that cannot render is a batch that cannot start.
      # ~3 s, dominated by browser launch.
      - name: Verify rasterisation reachable
        run: |
          set -euo pipefail
          url="https://alcohol-label-verify-staging.wing-lawrence.workers.dev"

          # Browser Rendering admits roughly 10 new instances per second, so a
          # launch can be refused for reasons that have nothing to do with this
          # revision. One retry, rather than failing a good deploy on contention.
          for attempt in 1 2; do
            if body=$(curl -fsS $GATE_AUTH --max-time 90 "$url/health/raster"); then break; fi
            echo "attempt $attempt: browser launch refused or timed out"
            body=$(curl -sS $GATE_AUTH --max-time 90 "$url/health/raster" || true)
            sleep 10
          done

          echo "$body"

          if echo "$body" | jq -e '.status == "ok"' > /dev/null; then
            # A zero-byte screenshot is a browser that launched and rendered
            # nothing — reported as ok, and useless.
            echo "$body" | jq -e '.bytes > 0' > /dev/null \
              || { echo "::error::rasterisation produced an empty image"; exit 1; }
          else
            reason=$(echo "$body" | jq -r '.error // .reason // "unknown"')
            # A rate limit is a fact about the account on the day, not about
            # this revision: Browser Rendering allows one new browser every 20
            # seconds and 10 minutes of browser time daily. Failing the deploy
            # for it would block shipping code that is entirely sound, and
            # would train people to re-run until it passes. Anything else — a
            # missing binding, a broken launch — still fails.
            if echo "$reason" | grep -qiE '429|rate limit|browser time limit'; then
              echo "::warning::rasterisation unverified: $reason"
              echo "::warning::the account is at its Browser Rendering limit; this does not indicate a problem with this revision"
            else
              echo "::error::/health/raster reported: $reason"
              exit 1
            fi
          fi

      # NFR-13 is a claim the audit record makes about itself: a verdict can be
      # produced again from what was stored. Nothing else in this gate would
      # notice it breaking. A rule changed without a version bump — exactly the
      # mistake versions exist to catch — turns every stored verdict into
      # fiction, silently, until someone thinks to check.
      - name: Verify stored verdicts still re-derive
        run: |
          set -euo pipefail
          url="https://alcohol-label-verify-staging.wing-lawrence.workers.dev"

          body=$(curl -sS $GATE_AUTH --max-time 60 "$url/audit/replay?limit=25")
          echo "$body" | jq '{checked, identical, differs, "not-comparable", "not-re-derivable", "record-altered"}'

          # Nothing to replay is not a pass. A fresh database would otherwise
          # report zero disagreements and read as verification.
          checked=$(echo "$body" | jq -r '.checked // 0')
          if [ "$checked" -eq 0 ]; then
            echo "::warning::no stored verdicts to replay — re-derivability unverified"
            exit 0
          fi

          # Only `differs` fails. A verdict recorded before the record carried
          # everything the comparison used cannot be re-derived and never will
          # be; counting those would leave the gate permanently red, which is
          # the same as having no gate.
          differs=$(echo "$body" | jq -r '.differs // 0')
          altered=$(echo "$body" | jq -r '.["record-altered"] // 0')

          # An altered reading is the graver of the two. A rule that moved is a
          # mistake in this revision; a reading whose digest no longer matches
          # the one committed to the chain means the stored evidence changed
          # after the verdict was written.
          if [ "$altered" -ne 0 ]; then
            echo "$body" | jq '.findings'
            echo "::error::$altered stored reading(s) no longer match the digest recorded in the audit chain"
            exit 1
          fi

          if [ "$differs" -ne 0 ]; then
            echo "$body" | jq '.findings'
            echo "::error::$differs stored verdict(s) no longer re-derive from the record"
            exit 1
          fi

          stale=$(echo "$body" | jq -r '.["not-re-derivable"] // 0')
          if [ "$stale" -ne 0 ]; then
            echo "::warning::$stale verdict(s) predate the record they would need; not re-derivable by design"
          fi

          # The same false pass as `checked -eq 0`, one level up, and this one
          # has already happened: bumping AGGREGATION_VERSION made every stored
          # verdict not-comparable, so this step reported success having
          # re-derived nothing. `differs` cannot be non-zero when nothing was
          # comparable, which is why the checks above stayed green through a
          # revision that reproduced no verdict at all.
          #
          # A warning rather than a failure, for the reason given above: after a
          # deliberate version bump this is the correct and expected state, and
          # failing would block every deploy until a fresh batch has been run.
          # It stops the step from reading as verification it did not perform.
          identical=$(echo "$body" | jq -r '.identical // 0')
          if [ "$identical" -eq 0 ]; then
            echo "::warning::$checked verdict(s) checked, none comparable — re-derivability unverified. Run a batch under this revision to confirm."
          fi
