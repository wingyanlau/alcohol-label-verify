name: CI

# Pull requests only. A push to main is covered by Deploy Staging, which runs
# the same gate before it deploys; running both would duplicate the suite on
# every merge and report two statuses for one revision.
on:
  pull_request:

jobs:
  quality:
    uses: ./.github/workflows/quality.yml
